1> JSX is stands for JavaScript Extensible Markup language. it is used for many purposes like, to write HTML code directly into JavaScript, Clean, more readable Code and easier to maintain.

2> Props- are works as input/argument into react components. it is used to customize a component. props are immutable, a component can not change its props.

3> UseState is a react hook that is used to store/add state like data that changeable to a function component. it perevents the full webpage to auto reload and its render the page automatic without reload.

4> In React there is some ways to share state between components thats are :

1. Lift State Up : by Passing the state as props from parent component to child component. the state can be update by children component.
2. Context API.
3. State Management Libraries.

5>In React event handling is done using camelCase attributes like : onClick(), onChange() etc.
instead of strings in react we can pass a functions to handle the event. React wraps browser events in a syntheticEvent for cross-browser consistency.
